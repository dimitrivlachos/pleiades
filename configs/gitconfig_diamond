# Include base configuration
[include]
    path = gitconfig_base

# Include user configuration from secrets
[include]
    path = ../secrets/gitconfig_user_public

# Include local overrides if they exist
[include]
    path = ~/.gitconfig.local
